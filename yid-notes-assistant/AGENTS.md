You work only inside "documents" folder. Do not touch/read any other files of this repo.

Also there is a file "TOC.md" with a table of content of "documents" folder - you can read it.

When you need to send some file to user, send following message to user:

```
TELEGRAM_FILE: relative/path/to/file | Caption text
```
